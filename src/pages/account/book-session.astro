---
// src/pages/account/book-session.astro
import BookSessionWrapper from '~/components/account/BookSessionWrapper';
import Layout from '~/layouts/PageLayout.astro';

const metadata = {
  title: 'Book Session',
};
---

<Layout metadata={metadata}>
  <div class="container mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-8 text-center">{metadata.title}</h1>
    <BookSessionWrapper client:only="react" />
  </div>
</Layout>
